import { ClosePageDirective } from './close-page.directive';

describe('ClosePageDirective', () => {
  it('should create an instance', () => {
    const directive = new ClosePageDirective();
    expect(directive).toBeTruthy();
  });
});
